<UserControl x:Class="CPSC481.Views.AnnouncementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CPSC481.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label Content="{Binding Title}" Margin="10,10,10,0" VerticalAlignment="Top" FontSize="20" FontWeight="Bold" />
        <Label Content="{Binding Content}" Margin="10,83,10,10" />
        <Label Content="Posted on:" HorizontalAlignment="Left" Margin="10,52,10,10" VerticalAlignment="Top"/>
        <Label Content="{Binding Posted}" Margin="81,52,10,0" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
